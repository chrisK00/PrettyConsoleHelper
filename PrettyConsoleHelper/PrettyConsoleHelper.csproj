<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net5.0</TargetFramework>
    <Authors>chrisK00</Authors>
    <Description>Library for Console apps with prettier and easier experience with output and input parsing/validating. Includes a Console table (rows and columns), Input helper and a console output class. Beginner friendly</Description>
    <Copyright>MIT</Copyright>
    <RepositoryUrl>https://github.com/chrisK00/PrettyConsoleOutput</RepositoryUrl>
    <PackageProjectUrl></PackageProjectUrl>
    <PackageTags>ConsoleTable, Input, Output, InputValidation, PrettyOutput, Table, OutputHelper, InputHelper, Console, beginner</PackageTags>
    <Version>1.0.2</Version>
    <PackageReleaseNotes>Now introducing PrettyConsoleOptions! Customize the pretty console and send it down to the input helper if youd like. I have wrapped them in a static class aswell if you dont want to bother customizing or you simply like the defaults.</PackageReleaseNotes>
    <PackageId>PrettyConsoleHelper</PackageId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="5.0.1" />
    <PackageReference Include="Microsoft.Extensions.Options" Version="5.0.0" />
  </ItemGroup>

</Project>
